<?xml version="1.0"?>
<configuration>
	<connectionStrings>
		<!-- please adjust the connection string embedded in the element below to target the proper catalog / server using the proper user / password combination -->
		<add name="ConnectionString.SQL Server (SqlClient)" connectionString="data source=ADMIN\SQLEXPRESS;initial catalog=violet;User ID=tuannm;Password=;persist security info=False"/>
	</connectionStrings>
</configuration> 